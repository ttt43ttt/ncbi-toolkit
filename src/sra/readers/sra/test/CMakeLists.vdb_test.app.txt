#############################################################################
# $Id: CMakeLists.vdb_test.app.txt 567016 2018-07-11 17:34:16Z dicuccio $
#############################################################################

NCBI_begin_app(vdb_test)
  NCBI_sources(vdb_test)
  NCBI_uses_toolkit_libraries(sraread)
  NCBI_project_watchers(vasilche ucko)
  NCBI_add_include_directories(${SRA_INCLUDE})
NCBI_end_app()

if(OFF)
#
#
#
add_executable(vdb_test-app
    vdb_test
)

set_target_properties(vdb_test-app PROPERTIES OUTPUT_NAME vdb_test)

include_directories(SYSTEM ${SRA_INCLUDE})

target_link_libraries(vdb_test-app
    sraread
)
endif()
