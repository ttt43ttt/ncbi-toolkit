#############################################################################
# $Id: CMakeLists.blast.lib.txt 566834 2018-07-09 12:39:41Z gouriano $
#############################################################################

NCBI_begin_lib(blast)
  NCBI_sources(${SRC_BLAST_C_CORE})
  NCBI_disable_pch()
  NCBI_uses_toolkit_libraries(composition_adjustment connect tables)
  NCBI_project_watchers(madden camacho fongah2)
NCBI_end_lib()

if(OFF)
#
#
#

include (CMakeLists.blast.objs.txt)

add_library(blast
    ${SRC_C}
)

target_link_libraries(blast
    composition_adjustment connect tables
)
endif()
