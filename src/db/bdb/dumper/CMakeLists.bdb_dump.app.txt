#############################################################################
# $Id: CMakeLists.bdb_dump.app.txt 565521 2018-06-13 13:04:30Z gouriano $
#############################################################################


NCBI_begin_app(bdb_dump)
  NCBI_sources(bdb_dumper)
  NCBI_uses_toolkit_libraries(bdb)
  NCBI_project_watchers(kuznets)
NCBI_end_app()

if(OFF)
#
#
#
add_executable(bdb_dump-app
    bdb_dumper
)

set_target_properties(bdb_dump-app PROPERTIES OUTPUT_NAME bdb_dump)

include_directories(SYSTEM ${BERKELEYDB_INCLUDE})

target_link_libraries(bdb_dump-app
    bdb
)
endif()
